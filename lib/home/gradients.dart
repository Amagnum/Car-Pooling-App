import 'package:flutter/material.dart';
import 'package:flutter/rendering.dart';

LinearGradient grat = LinearGradient(
    end: Alignment.bottomCenter,
    begin: Alignment.topCenter,
    stops: [0, 1],
    colors: [Colors.indigo, Color.fromRGBO(110, 97, 171, 1)]);

Color light = Color.fromRGBO(63, 0, 129, 1);
Color dark = Color.fromRGBO(110, 97, 171, 1);
